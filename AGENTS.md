# 저장소 작업 규칙

- 사용자의 파일 생성 또는 코드 수정 요청을 완료하면 변경사항을 검토한다.
- 검증에 문제가 없으면 `git add`, `git commit`, `git push`를 수행한다.
- 기본 push 대상은 `origin main`이다.
- 테스트 실패 시 commit과 push를 중단하고 결과를 보고한다.
- 삭제, force push, reset, rebase, 브랜치 병합은 사용자 확인 후 수행한다.
- 비밀정보와 환경설정 파일은 커밋하지 않는다.
